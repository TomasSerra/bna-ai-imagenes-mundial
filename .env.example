# No environment variables are required.
# This app uses BYOK (Bring Your Own Key): the user enters their BFL API key
# in the UI and it's stored in browser localStorage. The key never leaves the
# user's browser.
#
# Get your key at: https://api.bfl.ai
